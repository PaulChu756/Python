import math

class Boid:
	def_init_(self, pos, vel, speed, direction, time):
		self.pos = pos
		self.vel = vel
		self.s = speed
		self.dir = direction
		self.time = time
	
	def self.pos(x,y,z)
	return self.pos
	
	def self.vel(pos, time)
	return self.pos * self.time
	
	def self.speed(distance / time)
	return self.distance / self.time
	
	def self.direction(dir)
	return self.dir
	
	def magnitude(v):
    return math.sqrt(sum(v[i]*v[i] for i in range(len(v))))
''''''''''''magnited = vector.normalized
'''''''''''' direction = vector / size
